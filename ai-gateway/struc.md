┌──────────────────────────────┐
│          Web 前端            │
│  (业务管理界面 + AI 页面)   │
└─────────────┬────────────────┘
              │ HTTP 请求
              ▼
┌──────────────────────────────┐
│        ai-gateway (自研)     │
│  1. JWT 校验 (scope=ai)      │
│  2. API Key 校验 (X-AI-API-KEY) │
│  3. 权限判断 @RequirePermission │
│  4. RAG 查询业务 PG / vectorPG │
│  5. AI 请求日志落库 (sys_oper_log) │
└─────────────┬────────────────┘
              │ 转发模型请求
              ▼
┌──────────────────────────────┐
│           APISIX（网关产品）    │
│  1. 多模型管理               │
│  2. API Key / 限流 / 消耗统计│
│  3. 请求路由到 LLM           │
└─────────────┬────────────────┘
              │
              ▼
┌──────────────────────────────┐
│          LLM 模型服务        │
│   GPT-4、文心一格、Qwen 等   │
└──────────────────────────────┘

## 关于 API Key 管理：
- admin-server 负责 API Key 管理策略/配置

- AI 网关 负责运行时检查/限流/配额执行

- admin-server → 周期性推送/同步 API Key 配置到 AI 网关